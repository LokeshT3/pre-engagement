server:
  port: 8082
  
spring:
  datasource:
    url:
      jdbc:mysql://${MYSQL_HOST:localhost}:3306/patientdb
    username: root
    password: Lokesh01
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate.ddl-auto: update
    generate-ddl: true
    show-sql: true
